# Session encryption secret — must be at least 32 characters.
# Required in production. A development fallback is used if unset locally.
SESSION_SECRET=

# Node environment — set to "production" for deployed environments.
NODE_ENV=development
